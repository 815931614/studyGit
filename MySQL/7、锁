锁(自动加锁和释放锁)
    目的
        解决客户端并发访问冲突问题

    锁类型分类
        1、锁类型: 读锁 写锁
        2、锁颗粒: 行级锁(InnoDB) 表级锁(MyISAM)


        读锁(共享锁)：select加读锁之后别人不能更改表记录，但可以进行查询
        写锁(互斥锁,排他锁):加写锁之后别人不能查，不能改


    锁粒度分类
        表级锁: myisam
        行级锁: innodb
